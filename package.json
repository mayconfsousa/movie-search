{
  "name": "movie-search",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "cd packages/main-app && yarn start --port=$PORT",
    "build": "cd packages/main-app && yarn build"
  }
}
